## npm 常用包

- `morgan` -- expess的日志中间件，在开发时，控制台打印每次请求的基本信息，也可以记录到文件，按天切分日志文件；
- `got` -- 强大的http请求包，支持async...await
- `request` -- http请求包，使用很多
- `qs` -- 处理地址栏get参数的parse和stringify
- `node-cache` -- nodejs内置的key-value缓存
- `verdaccio ` -- npm私有仓库
- `forever ` -- 守护进程
- `pm2` -- 守护进程
